import SwiftUI

extension Color {
//    static let textColor = Color.primary
    static let textColor = Color.black
    static let flipBackground = Color(white: 0.95)
    static let separator = Color.gray.opacity(0.3)
}
